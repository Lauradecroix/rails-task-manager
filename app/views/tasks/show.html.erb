  <h2><%= @task.title %></h2>
  <p><%= @task.details %></p>
  <p><%= @task.completed %></p>

<%= link_to "Edit", edit_path%>
<%= link_to "Destroy", destroy_path, method: :delete, data: { confirm: "Are you sure?" }%>
<%= link_to "Back to list", tasks_path%>
