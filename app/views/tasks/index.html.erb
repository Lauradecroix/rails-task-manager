<div class="container">
<h1>My tasks</h1>

<% @tasks.each do |task| %>
  <%= link_to task.title, task_path(task) %>
  <p><%= task.details %></p>
<% end %>

<%= link_to("New task", new_path, class: 'btn btn-primary') %>

