<h1>My tasks</h1>

<% @tasks.each do |task| %>
  <%= link_to task.title, task_path(task) %>
  <p><%= task.details %></p>
  <p><%= task.completed %></p>
<% end %>

<%= link_to("New task", new_path) %>

